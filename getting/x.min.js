console.log('hello x.min.js')
function prechar(url, w, h) {
	var newImg = new Image(w, h)
	newImg.src = url

	newImg.onload = ()=>{
		console.log('Image charger dans la cache')
	}

	newImg.onerror = ()=>{
		console.log('erreur de chargement dans la cache')
	}
}

let a = 'http://127.0.0.1/video/_api_/get.php?item=use-data'
if (window.XMLHttpRequest) { // Mozilla, Safari, IE7+...
    httpRequest = new XMLHttpRequest();
}
else if (window.ActiveXObject) { // IE 6 et antérieurs
    httpRequest = new ActiveXObject("Microsoft.XMLHTTP");
}

httpRequest.onreadystatechange = () => {
	if (httpRequest.readyState === XMLHttpRequest.DONE) {
	    if (httpRequest.status === 200) {
		    console.log(httpRequest.responseText)
		    let J = JSON.parse(httpRequest.responseText)
		    console.log(J)
		    console.log(J['data'][0]['name'])
		} else {
			console.log('Desc')
		}
	}
}

httpRequest.open('GET', a+'&api=true', true);
httpRequest.send();